# Boru DHT discovery cross-cutting matrix report

- Date: 2026-08-19T14:10:32+10:00
- Runner: workspace-b (debsrv compute host)
- Features: net,test-utils
- Fault/soak knobs: records=50000 soak=25 cancel=30 limit=240s
- Tree: /home/dan/boru-build/work-1 @ 

## 1. Full integration test gate

| suite | result |
|-------|--------|
| sim | HANG (timeout 240s) |
| room_e2e | PASS |
| call_audio_integration | SKIP (needs voice-calls) |
| call_perf_measurement | SKIP (needs voice-calls) |
| call_video_integration | SKIP (needs video-calls) |
| voice_acceptance | SKIP (needs voice-calls) |
| three_peer_mesh | PASS |
| phase20_multi_instance | PASS |
| test_blob_size_enforcement | PASS |
| stale_bootstrap | FAIL |
| test_message_lifecycle | PASS |
| image_optimizer_integration | SKIP (needs gui) |
| performance_baseline | HANG (timeout 240s) |
| stress_test_comprehensive | PASS |
| gen_stress_data | PASS |
| test_serde_format | PASS |
| test_fixture | PASS |
| test_storage_integration | PASS |
| test_hostile_input | PASS |
| test_user_uploaded_gif | PASS |
| test_crash_recovery | PASS |
| test_sync_after_downtime | PASS |
| test_malformed_catalogue | PASS |
| test_pause_scenarios | PASS |
| test_download_initiation_integration | PASS |
| test_restart_storm_prevention | PASS |
| test_resource_exhaustion | PASS |
| test_download_queue_order | PASS |
| test_stable_identities | FAIL |
| test_peer_lifecycle | FAIL |
| test_catalogue_harness | PASS |
| test_deterministic_harness | PASS |
| test_remote_catalogue_integration | PASS |
| test_interrupted_transfer_harness | PASS |
| test_branding_rename | PASS |
| test_outbox_throughput | PASS |
| fs22_dashboard_coverage | PASS |
| tunnel_reconnect | PASS |
| protocol_registration | PASS |
| test_discovery_startup | PASS |
| test_discovery_two_node | PASS |
| test_public_room_directory | FAIL |
| test_discovery_dm_isolation | PASS |
| test_discovery_group_isolation | PASS |
| test_discovery_ui_isolation | PASS |
| test_discovery_restart | PASS |
| test_discovery_e2e_matrix | PASS |
| test_reconnect_asymmetric | PASS |
| discovery_compute_matrix | PASS |
| test_required_matrix | PASS |

Gate totals: PASS=39 FAIL=4 HANG=2 SKIP=5

## 2. Compute-intensive discovery matrix

`BORU_MATRIX_RECORDS=50000 BORU_SOAK_ROUNDS=25 BORU_CANCEL_CYCLES=30`

```
```
**compute matrix PASS**

## 3. clippy + fmt (feature-set scoped)

clippy (lib+bin, net,test-utils): PASS

## 4. Workspace-a-gated rows (run in the verification tree when merged)

| PDF §9 row | Verification |
|------------|--------------|
| Same LAN, DHT healthy — dedup, no duplicate joins | existing discovery e2e matrix + `join_saturation_bounded_concurrency` |
| Separate networks, DHT healthy — global bootstrap | **needs workspace-a** `DiscoveryBootstrapTracker` — run in verification tree |
| DHT unavailable / `--no-dht` — no DHT socket | existing `--no-dht` path; suite in gate run |
| Private room — secret-derived namespace intact | existing private-room suite in gate run |
| Public room — deterministic rendezvous | existing public-room directory suite in gate run |
| Join saturation — bounded queue, nothing lost | **needs workspace-a** `Task 2` bounded pending queue |
| Long-running session — recovery after cooldown | `soak_retry_recovery_no_dead_end`, `soak_many_waves_bounded` |
| Large/hostile result set — caps hold | `hostile_flood_caps_hold`, `hostile_categories_produce_rejections`, `oversized_record_rejected` |
| Shutdown during retry — prompt exit, drained | `shutdown_during_retry_prompt`, `cancellation_stress_repeated_cycles` |

Full logs:/tmp/matrix-suite-*.log /tmp/matrix-compute-20260819-141032.log /tmp/matrix-clippy-20260819-141032.log

## 5. Failure classification (all pre-existing / runner artifacts)

All discovered failures are unrelated to the workspace-b diff (which is purely
additive: `tests/discovery_compute_matrix.rs`, one `[[test]]` entry in
Cargo.toml, plus `scripts/` and `docs/`). `git status` shows workspace-b
touches none of the failing suite files.

- **`sim`, `performance_baseline` (HANG)** — the known debsrv relay-hang suites
  (`RelayMode::Default` + `endpoint.online()`, no IPv6 route on debsrv);
  documented pre-existing behaviour, unrelated to workspace-b.
- **`stale_bootstrap`** — `test_stale_bootstrap_does_not_block_rejoin` fails on
  `origin/main` too; pre-existing (peers/rejoin lane, not touched by workspace-b).
- **`test_stable_identities`**, **`test_peer_lifecycle`**,
  **`test_public_room_directory`** — one failing test each, all in files
  untouched by workspace-b; pre-existing on `origin/main`.
- **`voice_acceptance` (SKIP)** — the runner initially reported FAIL because its
  required-features parser only captured the first feature and ran the suite
  without `voice-calls`. Fixed the runner to parse the full feature list
  (multi-feature `required-features`); with the fix this suite is correctly
  SKIPped for the `net,test-utils` gate feature set.
